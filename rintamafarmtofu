-- Dọn dẹp script cũ nếu bạn bấm Execute nhiều lần
if getgenv().AutoFarmDelivery ~= nil then
    getgenv().AutoFarmDelivery = false
    task.wait(0.5) -- Đợi luồng cũ dừng hẳn
end

getgenv().AutoFarmDelivery = true

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer

-- Thông báo kích hoạt
pcall(function()
    game.StarterGui:SetCore("SendNotification", {
        Title = "Auto Farm",
        Text = "Script đang khởi động thành công!",
        Duration = 3
    })
end)

local Config = {
    StartPos = CFrame.new(-152.8, -21, 668.1),
    DeliveryOffset = CFrame.new(0, -10, 0),
    QuestName = "Fujiwara Bunta",
    WaitDelay = 0.5 -- Tăng nhẹ delay để tránh bị server nghi ngờ spam/kick
}

--==================================================
-- CHỐNG AFK KICK
--==================================================
pcall(function()
    for _, conn in pairs(getconnections(player.Idled)) do
        conn:Disable()
    end
end)

player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

--==================================================
-- HÀM BỔ TRỢ (UTILITIES)
--==================================================
local function getCharacterData()
    local char = player.Character or player.CharacterAdded:Wait()
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    return char, root, hum
end

local function teleportTo(targetCFrame)
    pcall(function()
        local _, root = getCharacterData()
        if root then
            root.CFrame = targetCFrame
        end
    end)
end

--==================================================
-- HỆ THỐNG ĐỨNG YÊN TRÊN KHÔNG (HOVER LOCK)
--==================================================
local function applyFloat()
    local _, root, hum = getCharacterData()
    if not root then return end

    if hum then
        hum.PlatformStand = true 
    end

    local bodyVel = root:FindFirstChild("FarmBodyVelocity")
    if not bodyVel then
        bodyVel = Instance.new("BodyVelocity")
        bodyVel.Name = "FarmBodyVelocity"
        bodyVel.MaxForce = Vector3.new(1e9, 1e9, 1e9)
        bodyVel.Velocity = Vector3.zero
        bodyVel.Parent = root
    end
end

if getgenv().FarmFloatConnection then
    getgenv().FarmFloatConnection:Disconnect()
end

getgenv().FarmFloatConnection = RunService.Heartbeat:Connect(function()
    if not getgenv().AutoFarmDelivery then
        -- Dọn dẹp trạng thái bay khi tắt script
        local _, root, hum = getCharacterData()
        if root then
            local bv = root:FindFirstChild("FarmBodyVelocity")
            if bv thenChào bạn, nếu script vẫn chưa chạy được sau khi gỡ bỏ các tính năng kiểm tra, nguyên nhân thường là do lỗi cú pháp hoặc code vẫn còn sót lại các tham chiếu đến phần đã bị xóa.
