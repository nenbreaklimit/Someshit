-- Tự động dọn dẹp các luồng cũ nếu bạn chạy lại script nhiều lần
if getgenv().AutoFarmLoop then task.cancel(getgenv().AutoFarmLoop) end
if getgenv().floatConnection then getgenv().floatConnection:Disconnect() end

getgenv().AutoFarmDelivery = true

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer

local Config = {
    StartPos = CFrame.new(-152.8, -21, 668.1),
    DeliveryOffset = CFrame.new(0, -10, 0),
    QuestName = "Fujiwara Bunta",
    WaitDelay = 0.2
}

--==================================================
-- THÔNG BÁO VÀ CHỐNG AFK (ANTI-AFK)
--==================================================
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Auto Farm Delivery",
        Text = "Script đã chạy thành công!",
        Duration = 3
    })
end)

pcall(function()
    for _, conn in pairs(getconnections(player.Idled)) do
        conn:Disable()
    end
end)

player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

--==================================================
-- UTILITIES (HÀM BỔ TRỢ)
--==================================================
local function getCharacterData()
    local char = player.Character or player.CharacterAdded:Wait()
    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    return char, root, hum
end

local function teleportTo(targetCFrame)
    pcall(function()
        local _, root = getCharacterData()
        if root then
            root.CFrame = targetCFrame
        end
    end)
end

--==================================================
-- KHÔNG BAO GIỜ RỚT (HOVER LOCK TỐI ƯU)
--==================================================
local bodyVel

local function applyFloat()
    local _, root, hum = getCharacterData()
    if not root then return end

    if hum then
        Script của bạn bị lỗi ở một vài chỗ khiến nó không thể chạy được. Dưới đây là các lỗi cụ thể đã được khắc phục:

1. **Lỗi cú pháp ở dòng 1:** Bạn dán nhầm chữ `Chủ đề mới, Local` vào phần khai báo biến `Players`.
2. **Thiếu hàm `getCharacterData()`:** Script gọi hàm này ở phần "HOVER LOCK" nhưng lại chưa định nghĩa nó ở trên, dẫn đến lỗi `attempt to call a nil value`.
3. **Gọi hàm `startFly()` không tồn tại:** Trong vòng lặp chính (MAIN LOOP), bạn gọi `startFly()` trong khi script không có hàm này (thực chất logic bay đã được xử lý ngầm qua `RunService.Heartbeat` ở phía trên).

Dưới đây là phiên bản đã được sửa lỗi và tối ưu hóa để có thể chạy mượt mà:

```lua
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer

--==================================================
-- EXECUTOR TOGGLE (Chạy 'false' để dừng script)
--==================================================
getgenv().AutoFarmDelivery = true

-- Gửi thông báo khi script chạy thành công
pcall(function()
    game.StarterGui:SetCore("SendNotification", {
        Title = "Auto Farm",
        Text = "Script Executed Successfully!",
        Duration = 3
    })
end)

local
